# =============================================================================
# rust-brain — Environment Configuration
# =============================================================================
# Copy to .env and customize for your environment.
# All values below are development defaults — change passwords for production.
# =============================================================================

# --- Project ---
COMPOSE_PROJECT_NAME=rustbrain

# --- Postgres ---
POSTGRES_USER=rustbrain
POSTGRES_PASSWORD=rustbrain_dev_2024
POSTGRES_DB=rustbrain
POSTGRES_PORT=5432
POSTGRES_URL=postgresql://rustbrain:rustbrain_dev_2024@postgres:5432/rustbrain

# --- Pgweb ---
PGWEB_PORT=8081

# --- Neo4j ---
NEO4J_AUTH=neo4j/rustbrain_dev_2024
NEO4J_PASSWORD=rustbrain_dev_2024
NEO4J_HTTP_PORT=7474
NEO4J_BOLT_PORT=7687
NEO4J_URI=bolt://neo4j:7687

# --- Qdrant ---
QDRANT_REST_PORT=6333
QDRANT_GRPC_PORT=6334
QDRANT_HOST=http://qdrant:6333
QDRANT_GRPC_HOST=http://qdrant:6334

# --- Ollama ---
OLLAMA_PORT=11434
OLLAMA_HOST=http://ollama:11434
EMBEDDING_MODEL=nomic-embed-text
EMBEDDING_DIMENSIONS=768
CODE_MODEL=codellama:7b

# --- Prometheus ---
PROMETHEUS_PORT=9090

# --- Grafana ---
GRAFANA_PORT=3000
GRAFANA_USER=admin
GRAFANA_PASSWORD=rustbrain

# --- Node Exporter ---
NODE_EXPORTER_PORT=9100

# --- Tool API ---
API_PORT=8088

# --- Logging ---
LOG_LEVEL=info
RUST_LOG=info,rustbrain=debug

# --- Ingestion ---
# Path to the Rust project to ingest (mounted into ingestion container)
INGEST_TARGET_PATH=./target-repo
# Maximum concurrent embedding requests to Ollama
EMBED_BATCH_SIZE=32
# Maximum concurrent Neo4j write transactions
NEO4J_BATCH_SIZE=1000
